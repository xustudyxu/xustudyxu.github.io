import { GlobbyOptions } from 'globby';
export declare type Replacer = [RegExp | string, string?];
export interface DehydratePluginOptions {
    noSSR?: string | string[];
    noScript?: string | string[];
    globOptions?: GlobbyOptions;
    noEmptyLine?: boolean;
}
