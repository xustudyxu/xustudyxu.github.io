"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.resolvePageFileContent = void 0;
const utils_1 = require("@vuepress/utils");
const log = (0, utils_1.debug)('vuepress:core/page');
/**
 * Resolve page file content according to filePath or options content
 */
const resolvePageFileContent = async ({ filePath, options, }) => {
    var _a;
    if (filePath) {
        try {
            // read page content from file
            const content = await utils_1.fs.readFile(filePath, 'utf-8');
            return content;
        }
        catch (e) {
            log(e instanceof Error ? e.message : e);
        }
    }
    // load raw content from options
    return (_a = options.content) !== null && _a !== void 0 ? _a : '';
};
exports.resolvePageFileContent = resolvePageFileContent;
