"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.resolvePluginModule = void 0;
const shared_1 = require("@vuepress/shared");
const utils_1 = require("@vuepress/utils");
/**
 * Resolve the module of a plugin according to name or path
 */
const resolvePluginModule = (pluginName) => {
    const pluginEntry = utils_1.path.isAbsolute(pluginName)
        ? pluginName
        : (0, utils_1.requireResolve)((0, shared_1.normalizePackageName)(pluginName, 'vuepress', 'plugin'));
    if (pluginEntry === null) {
        return null;
    }
    const pluginModule = require(pluginEntry);
    // allow default export
    return (0, utils_1.hasExportDefault)(pluginModule) ? pluginModule.default : pluginModule;
};
exports.resolvePluginModule = resolvePluginModule;
