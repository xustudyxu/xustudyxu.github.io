"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.resolveAppOptions = void 0;
const utils_1 = require("@vuepress/utils");
/**
 * Create app options with default values
 */
const resolveAppOptions = ({ 
// site config
base = '/', lang = 'en-US', title = '', description = '', head = [], locales = {}, 
// theme config
theme = '@vuepress/theme-default', themeConfig = {}, 
// bundler config
bundler = '@vuepress/bundler-vite', bundlerConfig = {}, 
// directory config
source, dest = utils_1.path.resolve(source, '.vuepress/dist'), temp = utils_1.path.resolve(source, '.vuepress/.temp'), cache = utils_1.path.resolve(source, '.vuepress/.cache'), public: publicDir = utils_1.path.resolve(source, '.vuepress/public'), 
// markdown config
markdown = {}, 
// development config
host = '0.0.0.0', port = 8080, debug = false, open = false, pagePatterns = ['**/*.md', '!.vuepress', '!node_modules'], templateDev = utils_1.path.normalize(require.resolve('@vuepress/client/templates/index.dev.html')), templateBuild = utils_1.path.normalize(require.resolve('@vuepress/client/templates/index.build.html')), shouldPreload = true, shouldPrefetch = false, 
// plugin config
plugins = [], }) => ({
    base,
    lang,
    title,
    description,
    head,
    locales,
    theme,
    themeConfig,
    bundler,
    bundlerConfig,
    source,
    dest,
    temp,
    cache,
    public: publicDir,
    markdown,
    debug,
    host,
    port,
    open,
    pagePatterns,
    templateDev,
    templateBuild,
    shouldPreload,
    shouldPrefetch,
    plugins,
});
exports.resolveAppOptions = resolveAppOptions;
