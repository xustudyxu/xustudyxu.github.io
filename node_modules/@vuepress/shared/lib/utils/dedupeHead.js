"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.dedupeHead = void 0;
const resolveHeadIdentifier_1 = require("./resolveHeadIdentifier");
/**
 * Dedupe head config with identifier
 *
 * Items that appear earlier have higher priority
 */
const dedupeHead = (head) => {
    const identifierSet = new Set();
    const result = [];
    head.forEach((item) => {
        const identifier = (0, resolveHeadIdentifier_1.resolveHeadIdentifier)(item);
        if (!identifierSet.has(identifier)) {
            identifierSet.add(identifier);
            result.push(item);
        }
    });
    return result;
};
exports.dedupeHead = dedupeHead;
