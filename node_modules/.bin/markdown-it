#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@vuepress/plugin-back-to-top/node_modules/markdown-it/bin/markdown-it.js" "$@"
  ret=$?
else 
  node  "$basedir/../@vuepress/plugin-back-to-top/node_modules/markdown-it/bin/markdown-it.js" "$@"
  ret=$?
fi
exit $ret
